// function find(arr, target) {
//   const index = Math.round(arr.length/2)
//   if (arr[index] === target) {
//     return index
//   } else if (arr[index] > target) {
//     return find(arr.slice(0, index), target)
//   } else {
//     return find(arr.slice(index), target) + index
//   }
// }


// const result = find([1,2,3,4,5,6], 6)
// console.log(result)

function find(arr, target) {
  
}